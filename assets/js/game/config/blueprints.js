// assets/js/game/config/blueprints.js

/**
 * Blueprints - это библиотека "чертежей" для всех игровых сущностей.
 * Каждый чертеж определяет базовые, неизменные свойства.
 * Сценарии и спаунеры будут использовать эти чертежи и дополнять их
 * уникальными деталями (позиция, визуал и т.д.).
 */
const Blueprints = {
    /**
     * BASE_THREAT - это самый простой шаблон для любой угрозы.
     * Он не содержит визуальной информации или начальной позиции,
     * только базовую игровую логику.
     */
    BASE_THREAT: {
        type: 'threat',
        hp_damage: 20, // Урон, наносимый игроку при столкновении
        movement: {
            pattern: 'flow',      // Прямолинейное движение
            direction: 'down',    // Направление движения
            speed: 150            // Скорость в пикселях в секунду
        }
    }
};